import controllers.LoginController;

/**
 * Entry point of the application
 * @author Antoine FORET
 * @version 1.0
 */
public class Main {

    /**
     * Entry point of the application.
     * @param attr parameters sent to the application (not used)
     */
    public static void main(String[] attr) {
        new LoginController();
    }
}
